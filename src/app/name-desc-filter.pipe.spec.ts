import { NameDescFilterPipe } from './name-desc-filter.pipe';

describe('NameDescFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NameDescFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
